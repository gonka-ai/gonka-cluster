# This file is managed by Ansible - DO NOT EDIT MANUALLY
# Docker UFW integration rules

# Allow Docker networks to communicate with each other
-A DOCKER-USER -i docker0 -o docker0 -j ACCEPT

# Allow traffic from Docker networks to external networks
-A DOCKER-USER -i docker0 ! -o docker0 -j ACCEPT

# Deny all other traffic to Docker containers by default
# (Our UFW rules will override this for allowed ports)
-A DOCKER-USER -j RETURN
