# This file is managed by Ansible - DO NOT EDIT MANUALLY
# Docker UFW integration rules for nftables systems
# Minimal rules to allow Docker functionality without interfering with nftables

# Allow established connections
-A DOCKER-USER -m conntrack --ctstate ESTABLISHED,RELATED -j ACCEPT

# Allow Docker bridge traffic
-A DOCKER-USER -i docker0 -o docker0 -j ACCEPT

# Allow traffic to Docker containers from allowed sources
# (UFW rules will handle specific port access)
-A DOCKER-USER -j RETURN
